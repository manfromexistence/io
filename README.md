# IO

```toml
[dependencies]
tokio-uring = "0.5"
memmap2 = "0.9"
rayon = "1.10"
tokio = { version = "1", features = ["full"] }
crossbeam-deque = "0.8"
libc = "0.2"
```

```bash
cargo add tokio-uring memmap2 rayon tokio crossbeam-deque libc
```